runtime: python312
entrypoint: uvicorn --port $PORT app:app
automatic_scaling:
  max_instances: 1
  max_concurrent_requests: 80
handlers:
- url: /.*
  script: auto
  secure: always
  redirect_http_response_code: 301
